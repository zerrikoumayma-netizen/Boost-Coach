# Decathlon Sport Buddy Frontend

React JSX frontend for the Spring Boot backend.

## Development

```bash
npm install
npm run dev
```

The app expects the backend API at `http://localhost:8081/api` by default. Override it with:

```bash
VITE_API_URL=http://localhost:8081/api
```

## Build

```bash
npm run build
```
